import { makeStyles, createStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() =>
  createStyles({
    countryListTitle: {
      textAlign: 'center',
    },
    countryList: {},
    countryCard: {},
    countryCardImage: {},
    countryCardButton: {},
  }),
);
